# ─────────────────────────────────────────────────────────
#  Invoxio — AI Agent Requirements
#  Python 3.13+
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────

# ── Google GenAI (Gemini) ─────────────────────────────────
google-generativeai>=0.8.0          # Official Google GenAI SDK
google-auth>=2.38.0                 # Google authentication

# ── LangChain Core ───────────────────────────────────────
langchain>=0.3.0                    # LangChain core framework
langchain-core>=0.3.0              # Base abstractions (messages, tools, runnables)
langchain-google-genai>=2.0.0      # LangChain × Google Gemini integration

# ── LangGraph ────────────────────────────────────────────
langgraph>=0.2.0                   # Stateful multi-agent graph framework
langgraph-checkpoint>=2.0.0        # Checkpointing for graph state persistence

# ── LangSmith (Tracing & Debugging) ──────────────────────
langsmith>=0.1.0                   # LangChain observability & tracing

# ── API Server (FastAPI) ──────────────────────────────────
fastapi>=0.115.0                   # Async Python web framework
uvicorn[standard]>=0.34.0          # ASGI server for FastAPI
sse-starlette>=2.1.0               # Server-Sent Events for streaming agent output
python-multipart>=0.0.20           # File upload support

# ── Database ─────────────────────────────────────────────
pymongo>=4.10.0                    # MongoDB driver
motor>=3.6.0                       # Async MongoDB driver (for FastAPI)
beanie>=1.27.0                     # Async ODM for MongoDB (Pydantic models)

# ── Data Validation ──────────────────────────────────────
pydantic>=2.10.0                   # Data validation & settings management
pydantic-settings>=2.6.0           # .env file loading with Pydantic

# ── Utilities ────────────────────────────────────────────
python-dotenv>=1.0.0               # Load .env variables
httpx>=0.28.0                      # Async HTTP client (used by LangChain)
tenacity>=9.0.0                    # Retry logic for API calls
python-jose[cryptography]>=3.3.0   # JWT token handling
passlib[bcrypt]>=1.7.4             # Password hashing

# ── Document Processing (Invoice OCR) ────────────────────
PyMuPDF>=1.24.0                    # PDF text extraction (fitz)
Pillow>=11.0.0                     # Image processing for invoice images
pytesseract>=0.3.13                # OCR engine wrapper (optional)

# ── Email ─────────────────────────────────────────────────
sendgrid>=6.11.0                   # SendGrid email API

# ── Testing ───────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
